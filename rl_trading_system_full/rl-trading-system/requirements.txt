# ─── Core (required) ──────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
yfinance>=0.2.28
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# ─── AI Sentiment Analysis — FinBERT (strongly recommended) ──────────────────
# Enables ProsusAI/finbert model; falls back to keyword scoring if absent
transformers>=4.40.0
# PyTorch CPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# PyTorch GPU (CUDA 12.1):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# ─── AI Portfolio Analyst — Groq (free tier) ─────────────────────────────────
# Free key at: console.groq.com — add to .env as GROQ_API_KEY=gsk_...
groq>=0.9.0

# ─── System monitoring (CPU/GPU utilization on dashboard) ─────────────────────
psutil>=5.9.0
nvidia-ml-py>=12.0.0     # NVIDIA GPU stats; optional — falls back to torch.cuda

# ─── Optional: visualization ──────────────────────────────────────────────────
matplotlib>=3.7.0
